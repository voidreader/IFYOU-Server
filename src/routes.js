// 글로벌
const HOME = "/";

// 클라이언트 처리
const CLIENT = "/client"; // 클라이언트 route 처리

const REP = `/rep`; // 네트워크 인사이트용

// 스토리

const routes = {
  home: HOME,
  clientApp: CLIENT,
  rep: REP,
};

export default routes;
